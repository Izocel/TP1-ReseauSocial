#include "Serveur.h"


ReponseServeur Serveur::parseRequete(RequeteClient& rqstCLient)
{
    if (rqstCLient.m_type == "POST") {

    }

    else if (rqstCLient.m_type == "GET") {

    }
    return ReponseServeur();
}
